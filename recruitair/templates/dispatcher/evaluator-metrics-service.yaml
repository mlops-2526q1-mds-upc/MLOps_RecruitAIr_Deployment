{{- if .Values.dispatcher.extractor.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dispatcher-extractor-metrics
  labels:
    {{- include "..labels" . | nindent 4 }}
spec:
  type: {{ .Values.dispatcher.extractor.metrics.service.type }}
  ports:
    - port: {{ .Values.dispatcher.extractor.metrics.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}-dispatcher-extractor
    {{- include "..selectorLabels" . | nindent 4 }}
{{- end }}