apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://{{ .Release.Name }}-prometheus:{{ .Values.prometheus.service.port }}/prometheus
    isDefault: true
